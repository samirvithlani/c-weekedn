//continers ---> vector, list, deque, stack, queue, priority_queue
//array --->

#include <iostream>
#include <vector>

using namespace std;

//v1 v2

int main(){
    vector <int> v;
    v.push_back(15);
    v.push_back(20);
    
    v.push_back(25);
    v.push_back(30);
    v.push_back(39);
    

    cout<<v.capacity()<<endl;
    //cout<<vector.max_size()<<endl;
    // cout << "Size of vector: " << v.size() << endl;
    // // cout<<"\n v[0] = " << v[0] << endl;

    // for(int i=0;i<v.size();i++){
    //     cout << v[i] << " ";
    // }
    // //remove
    // v.insert(v.begin()+1, 100);
    // v.pop_back();
    // cout<<"\n after erase: ";
    // for(int i=0;i<v.size();i++){
    //     cout << v[i] << " ";
    // }
   

    
}